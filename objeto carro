class Carro {
    int velocidade;
    int ano;
     String cor;
     String modelo;

     int aumentarVelocidade(int velocidadeAdicional){
         velocidade = velocidade + velocidadeAdicional;
         return velocidade;

     }
}
